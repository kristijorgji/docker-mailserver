{{ ansible_managed | comment }}

connect = host=127.0.0.1 dbname={{ mysql_mailserver_database }} user={{ mysql_mailserver_user }} password={{ mysql_mailserver_password }}

map {
  pattern = priv/quota/storage
  table = virtual_users
  username_field = email
  value_field = quota_bytes
}
